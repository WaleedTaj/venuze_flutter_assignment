/// Layout values taken from the 375 x 812 Figma frame.
abstract final class AppDimens {
  static const pagePadding = 35.0;
  static const radius = 8.0;
  static const fieldHeight = 43.0;
  static const socialButtonHeight = 49.0;

  /// Content never grows wider than this on tablets / landscape / web.
  static const maxContentWidth = 480.0;
}
